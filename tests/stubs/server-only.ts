/**
 * Заглушка пакета `server-only` для юнит-тестов.
 *
 * Настоящий пакет намеренно бросает исключение вне React Server Components,
 * поэтому Vitest подменяет его этим пустым модулем. На сборку приложения это
 * не влияет: alias объявлен только в vitest.config.ts.
 */
export {};
